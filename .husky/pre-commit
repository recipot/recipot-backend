# Node.js 환경 설정
if [ -f ~/.nvm/nvm.sh ]; then
  . ~/.nvm/nvm.sh
  nvm use
elif [ -f ~/.bashrc ]; then
  . ~/.bashrc
elif [ -f ~/.zshrc ]; then
  . ~/.zshrc
fi

# 린트 및 포맷팅 실행
npx lint-staged

# 단위 테스트 실행
echo "🧪 단위 테스트 실행 중..."
npm run test

# E2E 테스트 실행
echo "🧪 E2E 테스트 실행 중..."
npm run test:e2e
